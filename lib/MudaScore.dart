import 'package:flutter/material.dart';

class MudaScore extends StatelessWidget {
  const MudaScore({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}